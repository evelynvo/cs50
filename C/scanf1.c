// Gets a string from user using scanf

// this code is for example and does not work as it should

#include <stdio.h>

int main(void)
{
    char *s = NULL;
    printf("s: ");
    scanf("%s", s);
    printf("s: %s\n", s);
    return 0;
}
